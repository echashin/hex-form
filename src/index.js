'use strict';
$(document).ready(function () {
  //moment.locale('ru');
  //hex.form();
  //hexForm();
  /*
  hexForm('test').on('afterSubmit', function (event, data) {
    console.log(event, data);
  });
  */

});
